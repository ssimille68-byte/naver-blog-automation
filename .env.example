# 대시보드 포트 (기본 4300)
PORT=4300
HOST=127.0.0.1

# 데이터(세션·초안·이미지) 저장 위치. 기본값은 프로젝트의 data/ 폴더.
# NBA_DATA_DIR=/Users/me/naver-blog-data

# `claude` 실행 파일 경로를 직접 지정해야 할 때만 사용
# CLAUDE_CLI_PATH=/usr/local/bin/claude

# 동시에 띄울 Claude CLI 프로세스 수 (기본 2)
# NBA_AI_CONCURRENCY=2
