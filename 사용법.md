# 사용법

`npm run capture` 로 수집이 정상 동작하는 것을 확인한 다음 읽으세요.

---

## 0. 딱 한 가지 준비 — Claude 로그인

수집(`capture`)은 AI 가 필요 없지만, **글감 고르기·글쓰기·이미지 판단은 AI 가 합니다.**
이 프로그램은 `claude -p` 를 쓰므로 **이미 쓰고 있는 구독 요금제가 그대로 적용**됩니다. API 키는 필요 없습니다.

```bash
npm i -g @anthropic-ai/claude-code    # 이미 있으면 건너뛰기
claude login                          # 구독 계정으로 로그인
npm run doctor                        # 전부 갖춰졌는지 확인
```

`doctor` 에서 이렇게 나오면 준비 끝입니다.

```
✅ Node.js
✅ Claude Code CLI
✅ Playwright Chromium 준비됨
⚠️  네이버 세션 없음     ← 이건 다음 단계에서 해결합니다
```

---

## 1. 대시보드 켜기

```bash
npm start
```

브라우저에서 **http://127.0.0.1:4300** 을 엽니다.
`127.0.0.1` 은 내 컴퓨터만 접속할 수 있는 주소입니다. 밖에서는 못 들어옵니다.

---

## 2. 네이버 로그인 (최초 1회)

`자동 실행` 탭 → **[네이버 로그인]** 버튼.

브라우저 창이 하나 뜹니다. **평소처럼 직접 로그인하세요.** 2단계 인증, 캡차 전부 사람이 하는 게 맞습니다.
로그인이 확인되면 창이 닫히고 세션이 `data/browser-profile/` 에 저장됩니다. 다음부터는 생략됩니다.

> 상단 알약이 `네이버 (내아이디)` 로 바뀌면 성공입니다.
> 나중에 세션이 풀리면 `세션 확인` 을 눌러 보고, 만료됐으면 다시 로그인하면 됩니다.

---

## 3. 첫 글 만들기 — 안전한 순서

**처음에는 전자동 실행을 쓰지 마세요.** 한 단계씩 결과를 보면서 가는 편이 훨씬 낫습니다.

### ① 글감 찾기 (`글감` 탭)

관심 분야를 넣고 **[글감 찾기]**. 2~4분 걸립니다.

점수순으로 글감이 나옵니다. **[근거 자료 보기]** 를 눌러 어떤 뉴스·블로그를 보고 뽑은 것인지 꼭 확인하세요.
근거가 부실한 글감은 글도 부실해집니다.

### ② 글쓰기

마음에 드는 글감에서 **[이 글감으로 쓰기]**.
추가 요청 창이 뜹니다 — 비워 둬도 되고, 이렇게 적어도 됩니다.

```
초보자 기준으로 써 주세요. 가격 이야기는 빼고요.
```

원문을 읽고 → 글을 쓰고 → 이미지를 찾아 검증까지 합니다. **5~10분** 걸립니다.

### ③ 검토 (`초안` 탭)

왼쪽에서 초안을 고르면 오른쪽에 전체 글과 **AI 이미지 검증 결과**가 나옵니다.

```
82점 · Photographer · CC BY 2.0 · openverse
사진 설명: 원목 테이블 위 드리퍼와 원두가 담긴 유리병
판단: 본문의 원두 보관 주제와 직접 맞닿아 있음
```

여기서 **사실 확인은 사람이 해야 합니다.** 숫자·날짜·인용은 근거 자료와 대조하세요.

고칠 게 있으면 **[AI 수정 요청]** — 구체적으로 적을수록 잘 고칩니다.

| 이렇게 말고 | 이렇게 |
|---|---|
| 더 좋게 | 도입부 3문단을 1문단으로 줄이고 질문으로 시작해 주세요 |
| 이미지 별로 | 두 번째 이미지를 원두 클로즈업으로 바꿔 주세요 |
| 길어요 | 1200자 정도로 줄이되 소제목 구조는 유지해 주세요 |

### ④ 발행 연습 (중요)

**[발행 연습 (발행 안 함)]** 을 먼저 누르세요.
네이버 에디터를 열어 제목·본문·이미지를 실제로 채워 넣고, **발행 버튼은 누르지 않은 채** 화면을 캡처합니다.

`발행 기록` 탭에서 **[화면 캡처]** 를 눌러 확인하세요. 서식이 제대로 들어갔는지 눈으로 볼 수 있습니다.

> 💡 **첫 연습은 브라우저를 보면서 하세요.**
> `설정` 탭 → `발행 시 브라우저 숨기기` 체크 해제 → 저장.
> 글이 타이핑되고 사진이 올라가는 과정이 다 보입니다. 잘 되는 걸 확인했으면 다시 켜세요.
>
> ⚠️ 연습 모드는 **글을 저장하지 않고** 창을 닫습니다. 진짜 올릴 때는 발행 모드로 다시 돌려야 합니다.

### ⑤ 진짜 발행

카테고리와 공개 범위를 정하고 **[네이버에 발행]**.
처음에는 **비공개**로 올려 보고, 블로그에서 확인한 뒤 공개로 바꾸는 방법도 좋습니다.

---

## 4. 결과를 좋게 만드는 요령

### 관심 분야는 구체적으로

이 값으로 검색 키워드 5개를 만들기 때문에, 넓으면 글감도 뻔해집니다.

| ❌ 넓음 | ✅ 구체적 |
|---|---|
| 요리 | 에어프라이어 자취 요리 |
| 재테크 | 사회초년생 첫 적금 고르기 |
| 여행 | 제주 서쪽 당일치기 코스 |

### 톤을 내 목소리로 (`설정` → 글의 톤)

기본값은 무난하지만, 내 블로그 색깔로 바꾸면 결과가 확 달라집니다.

```
20~30대에게 말하듯 편한 반말체, 과장 없이 담백하게
```
```
경험을 먼저 이야기하고 정보는 뒤에 붙이는 식, 존댓말
```

### 분량

기본 1800자. 네이버 블로그는 **1500~2500자** 가 무난합니다. 3000자를 넘기면 AI 가 같은 말을 반복하기 시작합니다.

### 이미지가 자주 비어 있다면

기본 이미지 소스(Openverse·Wikimedia)는 **해외 자유 라이선스 사진 위주**입니다.
"홈카페", "러닝" 같은 보편적 소재는 잘 나오지만, **"부동산 세금", "청약" 같은 한국 특정 주제는 맞는 사진이 거의 없습니다.**

세 가지 중에 고르세요.

1. `설정` → `글 1편당 이미지 수` 를 **1** 로 줄이기 (가장 간단)
2. `AI 이미지 합격 점수` 를 70 → **55** 로 낮추기 (대신 덜 어울리는 사진이 섞임)
3. **Unsplash 무료 키** 발급받아 넣기 — unsplash.com/developers 에서 가입 후
   Access Key 를 `설정` 에 붙여넣고 이미지 소스에서 `unsplash` 체크

### 사용량 아끼기

글 1편에 AI 를 이만큼 씁니다.

| 단계 | 호출 수 |
|---|---|
| 글감 찾기 | 2회 |
| 글쓰기 | 1회 |
| **이미지 판독** | **최대 15회** ← 여기가 제일 큼 |

이미지 판독은 `이미지 수 × 후보 수` 만큼 돕니다(기본 3 × 5).
사용량이 부담되면 **`이미지 후보 수` 를 5 → 3** 으로 줄이세요. 체감 품질 차이는 적고 호출은 40% 줄어듭니다.

### 모델 선택

| 설정 | 추천 | 이유 |
|---|---|---|
| 글쓰기 모델 | `sonnet` | 품질·속도 균형. 중요한 글만 `opus` |
| 이미지 판독 모델 | `sonnet` | 느리면 `haiku` 로 낮춰도 괜찮음 |

---

## 5. 매일 쓰는 루틴

익숙해지면 이렇게 굴리면 됩니다.

```
아침   npm start  →  글감 찾기  →  괜찮은 글감 1~2개 골라 글쓰기
점심   초안 검토 + AI 수정 요청  →  발행 연습
저녁   최종 확인  →  발행
```

`자동 실행` 탭의 **[전자동 실행]** 은 글감 찾기부터 이미지까지 한 번에 돌립니다.
**검토 없이 바로 발행하는 체크박스는 웬만하면 켜지 마세요.** 확인 없이 올라간 글은 되돌리기 어렵습니다.

---

## 6. 꼭 지켜야 할 것

### 네이버 이용약관

자동화로 대량·반복 발행하면 **계정이 제재될 수 있습니다.**
- 하루 **1~2편**을 넘기지 마세요
- 발행 사이에 몇 시간씩 간격을 두세요
- 사람이 검토한 글만 올리세요

### 사실 확인은 사람 몫

AI 는 자료에 없는 내용을 지어내지 않도록 지시해 뒀지만, **완벽하지 않습니다.**
숫자·날짜·가격·법규는 반드시 원문과 대조하세요. 특히 의료·금융·법률 주제는 조심해야 합니다.

### 이미지 저작권

기본 소스는 자유 이용 라이선스지만 **출처 표기가 필요한 경우가 있습니다.**
초안 화면에 라이선스가 함께 표시되니 확인하고, 필요하면 글 끝에 출처를 남기세요.

---

## 7. 문제가 생기면

| 증상 | 해결 |
|---|---|
| 글감이 0건 | `npm run capture` → 리포트 확인. 네이버가 화면을 바꿨을 수 있음 |
| AI 가 응답 없음 | `npm run doctor` 로 Claude CLI 상태 확인 → `claude login` |
| 발행이 실패 | `data/logs/` 의 화면 캡처 확인. 에디터 구조가 바뀌었을 수 있음 |
| 로그인이 풀림 | 대시보드에서 `세션 확인` → 만료면 다시 로그인 |
| 코드를 최신으로 | `npm run update` |

`capture-report.md` 나 `data/logs/` 의 캡처 이미지를 보여 주면 원인을 찾을 수 있습니다.

---

## 아직 실제로 검증되지 않은 부분

솔직하게 적어 둡니다.

| 기능 | 상태 |
|---|---|
| 네이버 검색 수집 | ✅ 실제 네이버에서 확인 완료 |
| AI 글쓰기 | ✅ 확인 완료 |
| AI 이미지 판독 | ✅ 확인 완료 |
| 이미지 검색 | ⚠️ 실제 호출 미검증 |
| 네이버 로그인 | ⚠️ 미검증 |
| **네이버 발행** | ⚠️ **모의 에디터로만 검증** |

발행은 모의 에디터에서 제목·본문·이미지·서식·발행 레이어까지 전부 통과하지만,
**진짜 네이버 에디터에서 처음 돌리는 것은 이번이 처음**입니다.
그래서 **발행 연습을 먼저** 돌려 보라고 권하는 겁니다.
